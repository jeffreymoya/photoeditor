#!/bin/sh
set -e

echo "Running pre-push QA validation..."
echo ""
echo "This runs the full QA suite including tests, infra checks, and builds."
echo "Use SKIP_* env vars if you need to skip specific checks temporarily."
echo ""

# Run full QA suite before pushing
./scripts/qa/qa-suite.sh

echo ""
echo "Pre-push validation passed!"
echo "Pushing to remote..."
