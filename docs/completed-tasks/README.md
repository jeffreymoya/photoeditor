This directory archives completed task specifications.

Guidelines
- Files here are immutable historical records of completed work.
- Filenames mirror their original names under `tasks/`.
- New work discovery should ignore this directory.
- Use `scripts/pick-task.sh --complete` to finalize and archive a task.

