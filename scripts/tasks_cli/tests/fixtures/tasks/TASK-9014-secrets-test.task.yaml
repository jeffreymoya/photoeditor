id: TASK-9014
title: Task for testing secret detection
status: todo
priority: P2
area: infrastructure
description: |
  Task to test secret scanning and sensitive file detection.
  NOTE: No real secrets in this file, just patterns for testing.

scope:
  in:
    - Environment configuration (.env.example, .env.template)
    - AWS configuration (infrastructure/terraform.tfvars.example)
    - Test fixtures (test/fixtures/mock-credentials.json)
  out:
    - Real secrets (.env, .env.local, credentials.json)
    - Private keys (*.pem, *.key, *.p12)
    - Token files (.aws/credentials, .npmrc with auth tokens)

plan:
  - Step 1: Scan for common secret patterns
  - Step 2: Exclude actual secret files from context
  - Step 3: Include only example/template files
  - Step 4: Verify no real credentials in diffs

acceptance_criteria:
  - criteria: No real secrets in context artifacts
    standards_ref: N/A
  - criteria: Secret patterns detected and excluded
    standards_ref: N/A

validation:
  commands:
    - cmd: grep -r "API_KEY" . --exclude-dir=node_modules || true
      description: Scan for API key patterns (should find only examples)

deliverables:
  - description: Environment template
    path: infrastructure/.env.example
  - description: Mock credentials for tests
    path: test/fixtures/mock-credentials.json
